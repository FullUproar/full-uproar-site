{
  "name": "full-uproar-figma-plugin",
  "version": "1.0.0",
  "description": "Import Full Uproar website pages directly into Figma",
  "scripts": {
    "build": "esbuild code.ts --bundle --outfile=code.js --target=es2017",
    "watch": "esbuild code.ts --bundle --outfile=code.js --target=es2017 --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.0",
    "esbuild": "^0.20.0"
  }
}
